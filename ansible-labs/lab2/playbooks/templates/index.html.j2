<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ web_index_title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body { font-family: Arial, sans-serif; margin: 40px; }
    .card { border: 1px solid #eee; border-radius: 10px; padding: 24px; }
    h1 { margin-top: 0; }
    .muted { color: #666; font-size: 0.95rem; }
  </style>
</head>
<body>
  <div class="card">
    <h1>{{ web_index_title }}</h1>
    <p>{{ web_message }}</p>
    <p class="muted">Deployed at: {{ ansible_date_time.date }} {{ ansible_date_time.time }}</p>
    <p class="muted">Host: {{ inventory_hostname }} — IP: {{ ansible_default_ipv4.address }}</p>
  </div>
</body>
</html>
